Email: <%= current_user.email%></br>
<a  href="<%= edit_user_registration_path%>" class="btn btn-outline-success my-2 my-sm-0" >Editer votre profil</a>
</br>
</br>
</br>



<%if current_user.avatar.attached? %>

  <%= image_tag current_user.avatar, alt: 'avatar' , class:'file'%>

<%else%>

  <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>

  <h3>Changer d'avatar ?</h3>


<%= form_tag user_avatars_path(current_user.id), multipart: true do %>
  <%= file_field_tag :avatar , class:'file'%>
  <%= submit_tag "mettre à jour" %>
  <% end %>

<%end%>

<%= form_tag user_avatars_path(current_user.id), multipart: true do %>
  <%= file_field_tag :avatar , class:'file' %>
  <%= submit_tag "mettre à jour" %>
  <% end %>

